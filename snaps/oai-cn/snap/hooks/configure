#!/bin/sh
set -e 
echo "Configure hook "

snapctl stop --disable ${SNAP_NAME}  2>&1  || true
#snapctl stop --disable ${SNAP_NAME}.hssd  2>&1  || true
#snapctl stop --disable ${SNAP_NAME}.mmed  2>&1  || true
#snapctl stop --disable ${SNAP_NAME}.spgwd 2>&1  || true

$SNAP/init hss

$SNAP/init mme

$SNAP/init spgw



